<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="plugin.video.katan"
       name="Katan"
       version="0.0.1"
       provider-name="katan">
    <requires>
        <import addon="xbmc.python" version="3.0.0"/>
        <import addon="script.module.requests" version="2.22.0" optional="true"/>
        <!-- Not optional. It is a *binary* add-on - the copy on this Windows
             box is 21.5.24, platform windows-x86_64 - so it can never ship
             inside a <platform>all</platform> zip of pure Python. What can be
             done is to let Kodi fetch it: a required import is resolved from
             the official repository before Katan installs, on every platform,
             which turns "go and find InputStream Adaptive yourself" into
             nothing at all. 19.0.0 is a floor, so Kodi 19, 20 and 21 each get
             the build that matches them. -->
        <import addon="inputstream.adaptive" version="19.0.0"/>
    </requires>

    <extension point="xbmc.python.pluginsource" library="main.py">
        <provides>video audio</provides>
    </extension>

    <extension point="xbmc.subtitle.module" library="subtitles.py"/>

    <extension point="xbmc.service" library="service.py"/>

    <extension point="xbmc.addon.metadata">
        <summary lang="en_GB">Lightweight Netflix-style streaming front end</summary>
        <summary lang="he_IL">ממשק צפייה בסגנון נטפליקס, קל ומהיר</summary>
        <description lang="en_GB">A single lightweight add-on that brings together discovery (TMDB, Trakt, AniList), debrid playback (Real-Debrid, Premiumize, TorBox, AllDebrid), Israeli live TV and VOD, anime, and automatic Hebrew/English subtitles with an AI translation fallback. Built for weak devices: bounded concurrency, capped caches, no skin fork.</description>
        <description lang="he_IL">תוסף אחד וקל שמאחד גילוי תוכן, ניגון דרך שירותי דבריד, ערוצים ו-VOD ישראליים, אנימה, וכתוביות אוטומטיות בעברית ובאנגלית עם תרגום AI כגיבוי.</description>
        <platform>all</platform>
        <license>GPL-3.0-or-later</license>
        <language>en he</language>
        <assets>
            <icon>resources/media/icon.png</icon>
            <fanart>resources/media/fanart.jpg</fanart>
        </assets>
        <news>v0.0.1
- The update check reads GitHub Releases
- The search keyboard: arrows that move, and a way to speak
- Move the repository to GitHub Pages, and say why
- Sweep what an update leaves behind
- An install replaces, rather than copying over the top
- [verified] Harden playback subtitles and runtime
- Install the release onto a box, and clear the zips out afterwards
- [verified] Show subtitle accuracy for cached sources</news>
    </extension>
</addon>
